package com.ecommerce.config;

import org.springframework.context.annotation.Configuration;

/**
 * Mantida apenas para futuras configs de segurança adicionais.
 * (Não declare passwordEncoder aqui para evitar conflito com SecurityConfig)
 */
@Configuration
public class SecurityBeansConfig {
    // Sem beans por enquanto.
}